/**
 * portfolio — public API for the portfolio feature.
 *
 * Import from here, not from internal paths:
 *   ✅  import { HeroSection } from '@/features/portfolio'
 *   ❌  import { HeroSection } from '@/features/portfolio/components/HeroSection'
 *
 * Add new section exports here as the feature grows.
 */
export { HeroSection }    from './components/HeroSection'
export { TerminalVisual } from './components/TerminalVisual'
